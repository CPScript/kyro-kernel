#include "drivers/network.h"
#include "kernel.h"

void network_init(void) {
    print_message("Network driver initialized (stub)\n");
    // Network driver initialization would go here
    // For now, just a placeholder since its not needed
}